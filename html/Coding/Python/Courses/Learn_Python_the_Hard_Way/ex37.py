from os     import system
from time   import sleep
import sys

sys.path.insert(0,'./../Modules')
system('clear')

from prompts import name_prompt

"""
################################################
Symbols to better understand, mostly regarding loops

as          part of a with/as statement
assert      ensure something is true
break       stop the loop right away
class
continue    dont process more of the loop, do it again
del         deletes an entry from a dictionary
except      if an exception happens, do this
exec        run a string as python
finally     exceptions or not, finally do this no matter what
global      declare that you want a global variable
in          part of for-loops, also a test of X in Y.
is          like == to test equality
lambda      creates a short anonymouse function
pass        indicates the block is empty
raise       Raise an exception when things go wrong
return      exit the function with a return value
try         try this block, and if exception, go to except
with        with an expression as a variable, do this
yield       pause here and return to caller
none        represents "no value"
bytes       stores bytes of text, images, files, etc
dict        key value mapping
Str Chars:
            \\ \' \"
            \a Bell
            \b Backspace
            \f Formfeed
            \r Carriage
            \t Tab
            \v Vertical Tab
Math:
            ** power of             (2 ** 4)
            // floor division
            %  modulus
            <= less than/equal
            >= greater than/equal
            != not equal



################################################
"""


"""
################################################

################################################
"""